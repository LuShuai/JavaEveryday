package someTest;

public class RegexMatcher{
	/*

	public static boolean match(String str, String regex){
		if(regex == null || regex.length() == 0) 
			return str == null || str.length() == 0;
		
		if(regex.charAt(0) == '.'){
			if(regex.length() == 1)
				return str.length() == 1;
			if(regex.charAt(1) == '*'){
				for(int n = 2; n<regex.length(); n++){
					match(str, regex.substring(n,regex.length()));
				}
			}
			else{
				return match(str, regex.substring(1,regex.length()));
			}
		}
		else
		{
			if(regex.length() == 1)
				return (regex.charAt(0) == str.charAt(0)) && str.length() == 1;
			if(regex.charAt(1) == '*'){
				for(int )
			}
		}
	}
	
	public static void main(String[] args){
		// TODO Auto-generated method stub

	}
*/
}
