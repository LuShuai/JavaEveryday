package dp;

/**
 * Created by shuailu on 8/7/15.
 */

/*
Given a 2D array, find the longest consecutive path.
path can start any where in the array
return the length of the path
 */
public class LongestConsecutivePathIn2DArray {
    public static int findLongestPath(int[][] map) {
        if (map == null || map.length == 0 || map[0].length == 0) {
            return 0;
        }
        return 1;
    }
}
